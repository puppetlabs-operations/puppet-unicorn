# Configuration variables for <%= name %>. This file is being managed by
# Puppet; changes to this file will be reverted.

DAEMON="/usr/local/bin/unicorn"  # ideally ruby version could be set by a variable??

# This configuration information is needed if the application is being launched
# with bundler. The init script will need to chdir into the approot wehn 
# running `bundle exec` so that it can locate the Gemfile.
APPROOT="<%= approot %>"

NAME="<%= name %>"

ENV="<%= rack_env %>"
PIDFILE="<%= pidfile %>"
DAEMON_OPTS="-D -E ${ENV} -c <%= config %>"

export RAILS_ENV="$ENV"
export RACK_ENV="$ENV"
